---

spring:
  cloud:
    config:
      server:
        git:
          uri: ${vcap.services.demo-git.credentials.paramKey}
          # ONLY include username and pwd if the repo is private
          username: 
          password: 
  
security:
  user:
    name: {username}
    password: {password}
          
---

info:
  description: Training configuration repo