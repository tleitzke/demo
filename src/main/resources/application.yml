---

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Dstashek/demo.git
          # ONLY include username and pwd if the repo is private
          username: 
          password: 
  
security:
  user:
    name: {username}
    password: {password}
          
---

info:
  description: Training configuration repo